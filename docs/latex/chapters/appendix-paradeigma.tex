%% =============================================================================
%% chapters/appendix-paradeigma.tex
%% Appendix A: Paradeigma Clinical Simulation & Testbed Subsystem
%% =============================================================================

\chapter{Paradeigma Clinical Simulation Subsystem}
\label{app:paradeigma}

\section{Subsystem Overview \& Architectural Principles}
\label{sec:paradeigma_overview}

The \texttt{Paradeigma} subsystem constitutes Harmonia's dedicated clinical simulation environment, reference testbed, and resilience verification framework. In enterprise healthcare integration, testing against live clinical systems (such as hospital PAS, EMR, LMS, and RIS-PACS platforms) introduces substantial clinical risk, privacy constraints under health data regulations, and non-deterministic environment behavior. \texttt{Paradeigma} resolves these constraints by providing deterministic, stateful, and autonomous microservice emulators for each major hospital actor.

\subsection{Core Design Principles}

\begin{enumerate}
    \item \textbf{High-Fidelity HL7 v2 and MLLP Emulation}: Each simulator operates as an authentic network actor over Minimal Lower Layer Protocol (MLLP) over TCP/IP, adhering to the standard \texttt{<VT>...<FS><CR>} framing protocol and standard HL7 v2.4/v2.5 message structures.
    \item \textbf{Deterministic Synthetic Generation}: All clinical identities, patient demographics, accession numbers, lab observations, and imaging findings are generated using pseudorandom seed algorithms (\texttt{SeedRandom}). Given the same seed, the generator produces identical, reproducible clinical records across test runs.
    \item \textbf{Synchronous Acknowledgement Contracts}: Inbound and outbound interfaces strictly enforce HL7 acknowledgment contracts (\texttt{ACK\^{}R01}, \texttt{ACK\^{}O01}, \texttt{ACK\^{}A01}), requiring MSA-2 acknowledgment codes (\texttt{AA}, \texttt{AE}, \texttt{AR}) to reference the originating MSH-10 Message Control ID.
    \item \textbf{Autonomous and Orchestrated Operating Modes}: Simulators can execute either autonomously on configurable background timer schedules or deterministically driven via the \texttt{ParadeigmaScenarioEngine} REST API.
    \item \textbf{Integrated Fault Injection}: Every simulator embeds the \texttt{FailureSimulator} engine, permitting runtime injection of connection drops, acknowledgment timeouts, artificial latency, and application rejections for automated resilience verification.
\end{enumerate}

\section{ArchiMate Application \& Interface Landscape}
\label{sec:paradeigma_landscape}

Figure~\ref{fig:paradeigma_architecture} illustrates the ArchiMate 3.2 application component landscape for \texttt{Paradeigma} and its integration with Harmonia's \texttt{Pylai} interface gateway and \texttt{Petasos} messaging bus.

\begin{figure}[htbp]
    \centering
    \begin{adjustbox}{max width=\textwidth}
        \input{diagrams/fig-paradeigma-architecture.tex}
    \end{adjustbox}
    \caption{ArchiMate 3.2 Component Landscape for the Paradeigma Simulation Subsystem}
    \label{fig:paradeigma_architecture}
\end{figure}

The architecture partitions into five microservices:
\begin{itemize}
    \item \textbf{\texttt{paradeigma-pas}}: Simulates the Patient Administration System (PAS), initiating patient registration, admission, transfer, demographic updates, and discharge events over interface \texttt{PD-01}.
    \item \textbf{\texttt{paradeigma-emr}}: Simulates the Electronic Medical Record (EMR), receiving ADT notifications over \texttt{PD-05} and originating laboratory and diagnostic imaging orders over \texttt{PD-04}.
    \item \textbf{\texttt{paradeigma-lms}}: Simulates the Laboratory Information System (LMS), receiving ADT updates (\texttt{PD-06}) and pathology orders (\texttt{PD-08}), and transmitting structured LOINC pathology results over \texttt{PD-02}.
    \item \textbf{\texttt{paradeigma-rispac}}: Simulates the Radiology Information System \& PACS, receiving ADT updates (\texttt{PD-07}) and radiology orders (\texttt{PD-09}), and returning DICOM-linked diagnostic imaging reports over \texttt{PD-03}.
    \item \textbf{\texttt{paradeigma-scenarios}}: Orchestrates multi-system clinical workflows (Patient Journeys) and controls fault injection parameters across the simulation fleet via REST APIs.
\end{itemize}

\section{Formal Interface Catalogue (PD-01 through PD-09)}
\label{sec:paradeigma_interfaces}

Table~\ref{tab:paradeigma_interfaces} specifies the formal interface catalogue spanning ingress, egress, and routing channels across the Paradeigma simulation fleet and Harmonia.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l p{2.2cm} p{2.2cm} p{2.5cm} c p{2.5cm}}
\toprule
\textbf{ID} & \textbf{Source Actor} & \textbf{Target Actor} & \textbf{HL7 Message} & \textbf{Port} & \textbf{Trigger / Purpose} \\
\midrule
\texttt{PD-01} & \texttt{paradeigma-pas} & Harmonia Inbound & \texttt{ADT\^{}A01..A08} & 2101 & Patient Lifecycle Events \\
\texttt{PD-02} & \texttt{paradeigma-lms} & Harmonia Inbound & \texttt{ORU\^{}R01} & 2102 & LOINC Lab Results \\
\texttt{PD-03} & \texttt{paradeigma-rispac} & Harmonia Inbound & \texttt{ORU\^{}R01} & 2103 & Imaging Diagnostic Reports \\
\texttt{PD-04} & \texttt{paradeigma-emr} & Harmonia Inbound & \texttt{ORM\^{}O01} & 2104 & Lab \& Imaging Orders \\
\texttt{PD-05} & Harmonia Outbound & \texttt{paradeigma-emr} & \texttt{ADT\^{}A01..A08} & 2201 & EMR Demographics Fan-Out \\
\texttt{PD-06} & Harmonia Outbound & \texttt{paradeigma-lms} & \texttt{ADT\^{}A01..A08} & 2202 & LMS Demographics Fan-Out \\
\texttt{PD-07} & Harmonia Outbound & \texttt{paradeigma-rispac} & \texttt{ADT\^{}A01..A08} & 2203 & RIS Demographics Fan-Out \\
\texttt{PD-08} & Harmonia Outbound & \texttt{paradeigma-lms} & \texttt{ORM\^{}O01} & 2204 & Pathology Order Routing \\
\texttt{PD-09} & Harmonia Outbound & \texttt{paradeigma-rispac} & \texttt{ORM\^{}O01} & 2205 & Radiology Order Routing \\
\bottomrule
\end{tabularx}
\caption{Paradeigma Interface Catalogue and Port Mappings}
\label{tab:paradeigma_interfaces}
\end{table}

\subsection{MLLP Framing \& Transport Protocol}

All network interfaces (PD-01 through PD-09) communicate using the Minimal Lower Layer Protocol (MLLP) over dedicated persistent TCP/IP sockets. An MLLP payload is framed strictly as:
\begin{equation}
\langle\text{MLLP Frame}\rangle ::= \texttt{0x0B}\ (\text{Start-of-Block / \texttt{<VT>}}) + \langle\text{HL7 v2 Payload}\rangle + \texttt{0x1C}\ (\text{End-of-Block / \texttt{<FS>}}) + \texttt{0x0D}\ (\text{Trailer / \texttt{<CR>}})
\end{equation}

\subsection{Synchronous Acknowledgement Contract}

Every transmitted HL7 v2 message requires an immediate, synchronous HL7 acknowledgement (\texttt{ACK}) over the established TCP socket prior to socket closure. The acknowledgment status in \texttt{MSA-1} determines downstream transmission handling:
\begin{itemize}
    \item \textbf{\texttt{AA} (Application Accept)}: Indicates successful receipt, structural validation, and internal queue persistence. The sending client marks the transmission as successfully completed.
    \item \textbf{\texttt{AE} (Application Error)}: Indicates a recoverable or transient downstream processing condition. Triggering clients engage exponential backoff retry.
    \item \textbf{\texttt{AR} (Application Reject)}: Indicates an unrecoverable structural or schema violation (such as missing required \texttt{PID-3} or malformed segment delimiters). The message is immediately routed to the Dead Letter Queue without automatic retry.
\end{itemize}

\section{Hospital Simulator Microservice Specifications}
\label{sec:paradeigma_simulators}

\subsection{Patient Administration System Simulator (\texttt{paradeigma-pas})}

The \texttt{paradeigma-pas} service models an acute care hospital Patient Administration System responsible for master patient identity and encounter tracking.
\begin{itemize}
    \item \textbf{Internal State Engine}: Maintains an active in-memory patient directory transitioning through state phases: $\text{UNREGISTERED} \to \text{REGISTERED} \to \text{ADMITTED} \to \text{TRANSFERRED} \to \text{UPDATED} \to \text{DISCHARGED}$.
    \item \textbf{Autonomous Scheduling Modes}:
    \begin{itemize}
        \item \texttt{FIXED}: Emits patient events at a deterministic interval (default 5000\,ms).
        \item \texttt{RANDOM\_RANGE}: Emits events at random intervals within a bounded window (e.g., 2000--10000\,ms).
        \item \texttt{OFF}: Background timers disabled; events triggered exclusively via REST endpoints.
    \end{itemize}
    \item \textbf{REST Management Interface} (Port \texttt{8091}):
    \begin{itemize}
        \item \texttt{GET /api/pas/status}: Returns active configuration, simulated patient count, and message telemetry.
        \item \texttt{POST /api/pas/config}: Updates generation mode, delay intervals, and target MLLP endpoints.
        \item \texttt{POST /api/pas/register}, \texttt{POST /api/pas/admit}, \texttt{POST /api/pas/transfer}, \texttt{POST /api/pas/update}, \texttt{POST /api/pas/discharge}: Triggers discrete patient state transitions.
        \item \texttt{POST /api/pas/trigger-next}: Advances the next patient in the demographic queue through their lifecycle.
    \end{itemize}
\end{itemize}

\subsection{Electronic Medical Record Simulator (\texttt{paradeigma-emr})}

The \texttt{paradeigma-emr} service simulates clinician interaction within an Electronic Medical Record:
\begin{itemize}
    \item \textbf{Inbound Ingestion} (Port \texttt{2201}, \texttt{PD-05}): Receives inbound \texttt{ADT\^{}A01}--\texttt{A08} broadcasts fanned out from Harmonia, synchronizing local patient and encounter tables.
    \item \textbf{Order Placement Engine} (Outbound Port \texttt{2104}, \texttt{PD-04}): Generates synthetic \texttt{ORM\^{}O01} orders containing clinical indications, placer order numbers, priority flags (\texttt{STAT} vs. \texttt{ROUTINE}), and attending physician metadata.
    \item \textbf{REST API} (Port \texttt{8092}):
    \begin{itemize}
        \item \texttt{GET /api/emr/status}: Simulator health and received ADT counters.
        \item \texttt{POST /api/emr/orders/laboratory}: Originates pathology orders for registered patients.
        \item \texttt{POST /api/emr/orders/imaging}: Originates diagnostic imaging orders for registered patients.
        \item \texttt{GET /api/emr/patients}: Inspects locally synchronized patient registries.
    \end{itemize}
\end{itemize}

\subsection{Laboratory Information System Simulator (\texttt{paradeigma-lms})}

The \texttt{paradeigma-lms} service emulates clinical pathology and laboratory analyzer workflows:
\begin{itemize}
    \item \textbf{Inbound Ingestion}: Receives patient admissions on Port \texttt{2202} (\texttt{PD-06}) and lab orders on Port \texttt{2204} (\texttt{PD-08}).
    \item \textbf{Pathology Result Generation}: Synthesizes \texttt{ORU\^{}R01} observation messages containing multi-segment \texttt{OBX} records mapped to standard LOINC coding systems.
    \item \textbf{Clinical Pathology Catalog}: Supports standard diagnostic panels detailed in Table~\ref{tab:loinc_catalog}.
    \item \textbf{REST API} (Port \texttt{8093}):
    \begin{itemize}
        \item \texttt{GET /api/lms/status}: Simulator status and received order queues.
        \item \texttt{POST /api/lms/results/laboratory}: Triggers immediate generation and transmission of completed lab results to Harmonia Port \texttt{2102} (\texttt{PD-02}).
    \end{itemize}
\end{itemize}

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l l l l l}
\toprule
\textbf{Panel Code} & \textbf{Panel Description} & \textbf{LOINC Code} & \textbf{Observation Name} & \textbf{Reference Range} \\
\midrule
\texttt{CBC} & Full Blood Count & \texttt{718-7} & Haemoglobin & 130--180\,g/L \\
             &                   & \texttt{6690-2} & White Blood Cell Count & 4.0--11.0\,$\times 10^9$/L \\
             &                   & \texttt{777-3} & Platelet Count & 150--400\,$\times 10^9$/L \\
             &                   & \texttt{4544-3} & Haematocrit & 0.40--0.52\,L/L \\
             &                   & \texttt{789-8} & Red Blood Cell Count & 4.5--6.5\,$\times 10^{12}$/L \\
\midrule
\texttt{ELEC} & Electrolytes \& Urea & \texttt{2951-2} & Sodium & 135--145\,mmol/L \\
              &                      & \texttt{2823-3} & Potassium & 3.5--5.0\,mmol/L \\
              &                      & \texttt{2075-0} & Chloride & 95--105\,mmol/L \\
              &                      & \texttt{2028-9} & Bicarbonate & 22--29\,mmol/L \\
              &                      & \texttt{2160-0} & Creatinine & 60--110\,$\mu$mol/L \\
              &                      & \texttt{3094-0} & Urea & 2.5--7.8\,mmol/L \\
\midrule
\texttt{LFT} & Liver Function Tests & \texttt{1751-7} & Albumin & 35--50\,g/L \\
             &                      & \texttt{1975-2} & Total Bilirubin & 3--20\,$\mu$mol/L \\
             &                      & \texttt{1742-6} & Alanine Aminotransferase (ALT) & 10--40\,U/L \\
             &                      & \texttt{1920-8} & Aspartate Aminotransferase (AST) & 10--40\,U/L \\
             &                      & \texttt{6768-6} & Alkaline Phosphatase (ALP) & 30--120\,U/L \\
\midrule
\texttt{GLU} & Fasting Blood Glucose & \texttt{2345-7} & Glucose & 3.9--6.0\,mmol/L \\
\bottomrule
\end{tabularx}
\caption{Paradeigma Synthetic Pathology LOINC Catalog}
\label{tab:loinc_catalog}
\end{table}

\subsection{Radiology Information System \& PACS Simulator (\texttt{paradeigma-rispac})}

The \texttt{paradeigma-rispac} service simulates diagnostic imaging modalities and PACS archiving:
\begin{itemize}
    \item \textbf{Inbound Ingestion}: Receives patient demographic feeds on Port \texttt{2203} (\texttt{PD-07}) and radiology examination orders on Port \texttt{2205} (\texttt{PD-09}).
    \item \textbf{Diagnostic Report Synthesis}: Formulates structured textual radiology impressions and narratives (\texttt{OBX} value type \texttt{TX}) with associated performing radiologist credentials.
    \item \textbf{Imaging Modality Catalog}:
    \begin{itemize}
        \item \texttt{XR\_CHEST}: 2-View Chest Radiography (evaluating infiltrate, consolidation, cardiomegaly).
        \item \texttt{CT\_HEAD}: Non-contrast Cranial Computed Tomography (acute hemorrhage and midline shift assessment).
        \item \texttt{CT\_ABDOMEN}: Contrast-enhanced Abdominal/Pelvic CT (parenchymal organ integrity).
        \item \texttt{MRI\_BRAIN}: High-resolution Magnetic Resonance Neuroimaging.
        \item \texttt{US\_ABDOMEN}: Abdominal Diagnostic Ultrasonography.
    \end{itemize}
    \item \textbf{REST API} (Port \texttt{8094}):
    \begin{itemize}
        \item \texttt{GET /api/rispac/status}: Service health and pending examination list.
        \item \texttt{POST /api/rispac/results/imaging}: Generates and transmits finalized imaging report \texttt{ORU\^{}R01} messages to Harmonia Port \texttt{2103} (\texttt{PD-03}).
    \end{itemize}
\end{itemize}

\section{Synthetic Data Models \& Deterministic Generators}
\label{sec:paradeigma_models}

\subsection{Canonical Domain Entities}

The \texttt{paradeigma-common} module models realistic clinical workflows using five core profile structures:

\begin{enumerate}
    \item \textbf{\texttt{PatientProfile}}: Represents master patient demographics.
    \begin{itemize}
        \item \texttt{patientId} (MRN, e.g., \texttt{MRN-100421}), \texttt{nationalIdentifier} (e.g., Medicare/NHS identifier).
        \item Demographic attributes: \texttt{firstName}, \texttt{lastName}, \texttt{dateOfBirth}, \texttt{gender} (\texttt{M}, \texttt{F}, \texttt{O}).
        \item Contact \& Geo: \texttt{addressLine}, \texttt{city}, \texttt{state}, \texttt{postalCode}, \texttt{phoneNumber}, \texttt{email}.
        \item Next of Kin: \texttt{nextOfKinName}, \texttt{nextOfKinRelationship}, \texttt{nextOfKinPhone}.
    \end{itemize}
    \item \textbf{\texttt{VisitProfile}}: Represents encounter and admission context.
    \begin{itemize}
        \item \texttt{visitNumber} (FIN/Account ID, e.g., \texttt{VIS-900421}).
        \item Admission \& Discharge timestamps, \texttt{visitType} (\texttt{INPATIENT}, \texttt{OUTPATIENT}, \texttt{EMERGENCY}).
        \item Location context: \texttt{pointOfCare} (e.g., \texttt{WARD-3A}), \texttt{room} (\texttt{RM-12}), \texttt{bed} (\texttt{BED-02}), \texttt{facility} (\texttt{HOSP-MAIN}).
        \item Care Team: \texttt{attendingDoctorId}, \texttt{attendingDoctorName}, \texttt{admittingDiagnosis}.
    \end{itemize}
    \item \textbf{\texttt{OrderProfile}}: Captures clinical orders.
    \begin{itemize}
        \item Identifiers: \texttt{placerOrderNumber} (\texttt{ORD-300001}), \texttt{fillerOrderNumber}.
        \item Context: \texttt{orderType} (\texttt{PATHOLOGY}, \texttt{DIAGNOSTIC\_IMAGING}), \texttt{universalServiceId}, \texttt{universalServiceText}.
        \item Clinical attributes: \texttt{priority} (\texttt{R} for Routine, \texttt{S} for Stat), \texttt{orderDateTime}, \texttt{clinicalIndication}.
    \end{itemize}
    \item \textbf{\texttt{ResultProfile}}: Encapsulates diagnostic report outputs.
    \begin{itemize}
        \item Order correlation: \texttt{placerOrderNumber}, \texttt{fillerOrderNumber}, \texttt{resultStatus} (\texttt{F} for Final, \texttt{P} for Preliminary, \texttt{C} for Corrected).
        \item Narrative report text: \texttt{diagnosticReportNarrative}.
        \item Observations: \texttt{List<ObservationItem>}.
    \end{itemize}
    \item \textbf{\texttt{ObservationItem}}: Represents granular test observations mapped to HL7 \texttt{OBX} segments.
    \begin{itemize}
        \item \texttt{setSubId}: Sequential identifier (\texttt{OBX-1}).
        \item \texttt{valueType}: \texttt{NM} (Numeric), \texttt{ST} (String), \texttt{TX} (Text), or \texttt{CE} (Coded).
        \item \texttt{observationId}: Standard LOINC or internal test code (\texttt{OBX-3.1}).
        \item \texttt{observationText}: Observation description (\texttt{OBX-3.2}).
        \item \texttt{codingSystem}: Coding system identifier, defaults to \texttt{LN} (\texttt{OBX-3.3}).
        \item \texttt{value}: Quantitative or qualitative test result value (\texttt{OBX-5}).
        \item \texttt{units}: Measurement units (\texttt{OBX-6}).
        \item \texttt{referenceRange}: Reference normal boundary (\texttt{OBX-7}).
        \item \texttt{abnormalFlags}: \texttt{N} (Normal), \texttt{H} (High), \texttt{L} (Low), \texttt{A} (Abnormal) (\texttt{OBX-8}).
        \item \texttt{resultStatus}: Observation status flag (\texttt{OBX-11}).
    \end{itemize}
\end{enumerate}

\subsection{Seed-Based Deterministic Generation Engine}

The \texttt{SeedRandom} utility wraps Java's pseudorandom generation algorithms with strict seed encapsulation. This guarantees that given an identical seed (e.g., patient seed \texttt{100421L}), generators produce identical clinical datasets across distributed test runners:

\begin{lstlisting}[language=Java, caption={SeedRandom Deterministic Generation Pattern in Paradeigma}]
public class SyntheticPatientGenerator {
    private final SeedRandom random;

    public SyntheticPatientGenerator(long seed) {
        this.random = new SeedRandom(seed);
    }

    public PatientProfile generatePatient(String patientId) {
        Gender gender = random.nextBoolean(0.5) ? Gender.FEMALE : Gender.MALE;
        String firstName = random.pick(gender == Gender.FEMALE ? FEMALE_NAMES : MALE_NAMES);
        String lastName = random.pick(LAST_NAMES);
        LocalDate dob = LocalDate.now().minusYears(random.nextInt(18, 85))
                                   .minusDays(random.nextInt(1, 365));
        return new PatientProfile(patientId, firstName, lastName, dob, gender);
    }
}
\end{lstlisting}

\section{Scenario Orchestration Engine \& Patient Journey Workflows}
\label{sec:paradeigma_scenarios}

The \texttt{ParadeigmaScenarioEngine} (microservice \texttt{paradeigma-scenarios}, Port \texttt{8090}) orchestrates end-to-end, multi-actor clinical workflows across the simulation testbed.

\subsection{9-Step Canonical Patient Journey Sequence}

A complete synthetic encounter exercises all ingress, egress, routing, and transformation pathways across Harmonia in a deterministic 9-step progression:

\begin{enumerate}
    \item \textbf{Step 1: Patient Registration (\texttt{PAS} $\to$ \texttt{ADT\^{}A04})}: \texttt{paradeigma-pas} registers a new synthetic patient with full demographic and Next-of-Kin details, transmitting over \texttt{PD-01} (:2101).
    \item \textbf{Step 2: Inpatient Admission (\texttt{PAS} $\to$ \texttt{ADT\^{}A01})}: \texttt{paradeigma-pas} assigns the patient to an acute inpatient ward (\texttt{WARD-3A}, \texttt{RM-12}, \texttt{BED-02}) and emits an admission event. Harmonia fans out \texttt{ADT\^{}A01} to \texttt{EMR} (\texttt{PD-05}), \texttt{LMS} (\texttt{PD-06}), and \texttt{RIS-PAC} (\texttt{PD-07}).
    \item \textbf{Step 3: Pathology Order Placement (\texttt{EMR} $\to$ \texttt{ORM\^{}O01})}: \texttt{paradeigma-emr} generates a Full Blood Count (\texttt{CBC}) lab order, transmitting over \texttt{PD-04} (:2104). Harmonia routes this order to \texttt{paradeigma-lms} over \texttt{PD-08} (:2204).
    \item \textbf{Step 4: Pathology Result Ingestion (\texttt{LMS} $\to$ \texttt{ORU\^{}R01})}: \texttt{paradeigma-lms} analyzes the specimen and produces a multi-line LOINC pathology result (\texttt{718-7}, \texttt{6690-2}, \texttt{777-3}), transmitting to Harmonia over \texttt{PD-02} (:2102).
    \item \textbf{Step 5: Diagnostic Imaging Order Placement (\texttt{EMR} $\to$ \texttt{ORM\^{}O01})}: \texttt{paradeigma-emr} requests a 2-view Chest X-Ray (\texttt{XR\_CHEST}) over \texttt{PD-04} (:2104). Harmonia routes the radiology order to \texttt{paradeigma-rispac} over \texttt{PD-09} (:2205).
    \item \textbf{Step 6: Diagnostic Imaging Result Ingestion (\texttt{RIS-PAC} $\to$ \texttt{ORU\^{}R01})}: \texttt{paradeigma-rispac} returns a structured diagnostic report with impression and findings narrative over \texttt{PD-03} (:2103).
    \item \textbf{Step 7: Inpatient Transfer (\texttt{PAS} $\to$ \texttt{ADT\^{}A02})}: \texttt{paradeigma-pas} transfers the patient to a step-down ward (\texttt{WARD-4B}, \texttt{RM-401}) and broadcasts \texttt{ADT\^{}A02} across all clinical destinations.
    \item \textbf{Step 8: Patient Demographics Update (\texttt{PAS} $\to$ \texttt{ADT\^{}A08})}: Updates patient contact information and address, triggering fan-out reconciliation across EMR, LMS, and RIS-PAC.
    \item \textbf{Step 9: Patient Discharge (\texttt{PAS} $\to$ \texttt{ADT\^{}A03})}: Emits encounter close and discharge disposition over \texttt{PD-01}, concluding the patient episode.
\end{enumerate}

\subsection{Execution Profiles}

The scenario engine supports three operational execution profiles:
\begin{itemize}
    \item \textbf{\texttt{TEST}}: Deterministic execution with minimal step pacing (10--50\,ms delay). Optimized for continuous integration pipelines and automated regression test suites.
    \item \textbf{\texttt{DEMO}}: Human-observable pacing with 2000--10000\,ms intervals between steps, designed for live demonstrations and interactive operational console monitoring.
    \item \textbf{\texttt{LOAD}}: Concurrent execution across configurable worker thread pools without synthetic pacing delays, evaluating platform throughput, queue depth, and memory footprint.
\end{itemize}

\subsection{Scenario Control REST API}

Table~\ref{tab:scenario_api} documents the REST control endpoints exposed on port \texttt{8090}.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l l p{6.5cm}}
\toprule
\textbf{HTTP Method} & \textbf{Endpoint Path} & \textbf{Operational Function} \\
\midrule
\texttt{POST} & \texttt{/api/scenarios/journey} & Triggers an immediate single 9-step Patient Journey. \\
\texttt{POST} & \texttt{/api/scenarios/concurrent} & Initiates $N$ parallel journey executions under specified profile. \\
\texttt{POST} & \texttt{/api/scenarios/continuous} & Starts sustained continuous journey background generation. \\
\texttt{POST} & \texttt{/api/scenarios/stop} & Gracefully halts active background scenarios and thread pools. \\
\texttt{GET}  & \texttt{/api/scenarios/status} & Queries execution telemetry, success rates, and active threads. \\
\bottomrule
\end{tabularx}
\caption{Scenario Engine REST Control Endpoints}
\label{tab:scenario_api}
\end{table}

\section{Failure Simulation, Fault Injection \& Automated Recovery}
\label{sec:paradeigma_failure}

\subsection{Fault Injection Modes (\texttt{FailureSimulator})}

The \texttt{FailureSimulator} embeds within each simulator's MLLP pipeline, permitting dynamic simulation of network anomalies and application failures without modifying underlying platform infrastructure. Table~\ref{tab:fault_modes} enumerates the supported failure modes configured via \texttt{FaultInjectionConfig}.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l c p{7.5cm}}
\toprule
\textbf{Fault Injection Parameter} & \textbf{Range / Type} & \textbf{Simulated Physical Failure Condition} \\
\midrule
\texttt{dropConnectionProbability} & $0.0 - 1.0$ & TCP connection RST / network partition mid-frame. \\
\texttt{noAckProbability} & $0.0 - 1.0$ & Destination unresponsiveness causing socket read timeout. \\
\texttt{ackDelayMs} & $\ge 0$\,ms & Network congestion or downstream processing latency. \\
\texttt{applicationErrorProbability} & $0.0 - 1.0$ & Returns \texttt{MSA-1=AE} simulating transient system error. \\
\texttt{applicationRejectProbability} & $0.0 - 1.0$ & Returns \texttt{MSA-1=AR} simulating schema/rule validation error. \\
\texttt{malformedSegmentProbability} & $0.0 - 1.0$ & Corrupts HL7 segment delimiters or required fields. \\
\texttt{duplicateMsh10Probability} & $0.0 - 1.0$ & Replays previous \texttt{MSH-10} to evaluate deduplication filters. \\
\bottomrule
\end{tabularx}
\caption{FailureSimulator Fault Injection Capabilities}
\label{tab:fault_modes}
\end{table}

\subsection{Automated Recovery Acceptance Test Suite}

The integration test suite in \texttt{paradeigma-test} (\texttt{RecoveryAcceptanceTest.java}) automates formal recovery verification across six critical failure scenarios:

\begin{enumerate}
    \item \textbf{Test 1 --- Broker Journal Recovery}: Verifies that unacknowledged in-flight messages persist across complete ActiveMQ Artemis broker crashes and restarts, replaying cleanly to consumers upon broker recovery without data loss.
    \item \textbf{Test 2 --- Ponos Worker Crash Recovery}: Simulates worker termination during Camel pipeline execution. Validates that uncommitted Artemis transactions redeliver to healthy worker instances, advancing the associated \texttt{Pragma} to \texttt{COMPLETED}.
    \item \textbf{Test 3 --- Outbound Destination Outage \& Backoff Recovery}: Simulates an LMS outage during ORM order transmission. Verifies that Pylai engages exponential retry backoff, retaining the message until LMS recovers and returns \texttt{ACK\^{}AA}.
    \item \textbf{Test 4 --- Duplicate HL7 MSH-10 Idempotency}: Transmits identical \texttt{MSH-10} control IDs twice. Validates that Petasos deduplication filters intercept the re-transmission, returning an idempotent \texttt{AA} acknowledgment without triggering duplicate task executions.
    \item \textbf{Test 5 --- Partial ADT Fan-Out Isolation}: Simulates an ADT fan-out where EMR and LMS succeed but RIS-PAC fails. Verifies that upon RIS-PAC restoration, only the pending RIS-PAC delivery is retried, avoiding duplicate broadcasts to EMR or LMS.
    \item \textbf{Test 6 --- Platform Cold Restart Recovery}: Queues multiple clinical transactions and performs a hard platform stop and restart. Verifies that all persistent journal messages and cache state reconstruct cleanly to terminal completion.
\end{enumerate}

\section{Deployment Topology \& Docker Orchestration}
\label{sec:paradeigma_deployment}

The Paradeigma simulation fleet deploys seamlessly alongside Harmonia using Docker Compose (\texttt{docker-compose-paradeigma.yml}). Table~\ref{tab:paradeigma_containers} outlines the container topology, port bindings, and health checks.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l l c c p{4.0cm}}
\toprule
\textbf{Container Service} & \textbf{Base Image} & \textbf{REST Port} & \textbf{MLLP Ports} & \textbf{Health Check Dependency} \\
\midrule
\texttt{paradeigma-pas} & OpenJDK 17 & 8091 & --- & \texttt{http://localhost:8091/api/pas/status} \\
\texttt{paradeigma-emr} & OpenJDK 17 & 8092 & 2201 & \texttt{http://localhost:8092/api/emr/status} \\
\texttt{paradeigma-lms} & OpenJDK 17 & 8093 & 2202, 2204 & \texttt{http://localhost:8093/api/lms/status} \\
\texttt{paradeigma-rispac} & OpenJDK 17 & 8094 & 2203, 2205 & \texttt{http://localhost:8094/api/rispac/status} \\
\texttt{paradeigma-scenarios} & OpenJDK 17 & 8090 & --- & Depends on PAS, EMR, LMS, RIS-PAC healthy \\
\bottomrule
\end{tabularx}
\caption{Paradeigma Docker Compose Deployment Configuration}
\label{tab:paradeigma_containers}
\end{table}

% =============================================================================
% SECTION A.9: PROVIDER REGISTRY SIMULATION & SYNTHETIC FHIR R5 GENERATORS
% =============================================================================
\section{Provider Registry Simulation \& Synthetic FHIR R5 Generators}
\label{sec:paradeigma_provider_registry}

With the introduction of the Harmonia FHIR R5 Provider Registry core services (Appendix~\ref{app:provider_registry}), \texttt{Paradeigma} expands beyond legacy HL7 v2 messaging to provide high-fidelity, deterministic synthetic generation of HL7 FHIR Release 5 directory resources.

\subsection{Standardized Australian Digital Health Identifiers}

All synthetic directory entities adhere strictly to canonical Australian digital health identifier schemas:
\begin{itemize}
    \item \textbf{Healthcare Provider Identifier -- Individual (HPI-I)}: Formatted under system URI \texttt{http://ns.electronichealth.net.au/id/hi/hpii/1.0} with standard prefix \texttt{800361} followed by 10 deterministic pseudorandom digits and a Luhn check digit (e.g., \texttt{8003610000000042}).
    \item \textbf{Healthcare Provider Identifier -- Organisation (HPI-O)}: Formatted under system URI \texttt{http://ns.electronichealth.net.au/id/hi/hpio/1.0} with prefix \texttt{800362} (e.g., \texttt{8003620000000042}).
    \item \textbf{AHPRA Registration Numbers}: Australian Health Practitioner Regulation Agency practitioner licenses (system \texttt{http://hl7.org.au/id/ahpra-registration-number}, e.g., \texttt{MED0001234567}).
    \item \textbf{SNOMED CT Specialty Codes}: Standardized Australian clinical specialties and role classifications (system \texttt{http://snomed.info/sct}, e.g., \texttt{309988001} for General Medical Practice).
\end{itemize}

\subsection{Dedicated Synthetic Generators Catalog}

The package \texttt{net.fhirfactory.harmonia.paradeigma.common.generator} provides seven dedicated, seed-controlled generators corresponding to each managed FHIR R5 resource type:

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l l X}
\toprule
\textbf{Generator Class} & \textbf{Target Resource} & \textbf{Key Synthetic Capabilities \& Variations} \\
\midrule
\texttt{PractitionerGenerator} & \texttt{Practitioner} & Deterministic human names, HPI-I, AHPRA credentials, qualification codings, telecom endpoints, address history, version updates, and duplicate identifier conflict models. \\
\addlinespace
\texttt{PractitionerRoleGenerator} & \texttt{PractitionerRole} & Specialty SNOMED codes, role classifications, multi-target referential linkages (\texttt{Organization}, \texttt{Location}, \texttt{HealthcareService}, \texttt{Endpoint}), and availability schedules. \\
\addlinespace
\texttt{OrganizationGenerator} & \texttt{Organization} & Hospital network names, HPI-O identifiers, Australian Business Numbers (ABN), facility types, physical campuses, and postal routing addresses. \\
\addlinespace
\texttt{LocationGenerator} & \texttt{Location} & Physical hospital wards, clinical wings, operating suites, physical type codes (\texttt{bu}, \texttt{wi}, \texttt{ro}), and managing organization references. \\
\addlinespace
\texttt{HealthcareServiceGenerator} & \texttt{HealthcareService} & Clinical offerings, consultation specialties, operational hours, referral requirements, and parent organization linkages. \\
\addlinespace
\texttt{EndpointGenerator} & \texttt{Endpoint} & Technical communication channels across FHIR REST (\texttt{hl7-fhir-rest}), HL7 v2 MLLP (\texttt{direct-project}), and secure messaging endpoints with payload mime-types. \\
\addlinespace
\texttt{GroupGenerator} & \texttt{Group} & Practitioner specialist panels, clinical governance committees, on-call rosters, and enumerated clinical team memberships. \\
\bottomrule
\end{tabularx}
\caption{Dedicated FHIR R5 Synthetic Resource Generators}
\label{tab:paradeigma_fhir_generators}
\end{table}

\subsection{Interconnected Graph Builders (\texttt{SyntheticProviderRegistryGenerator})}

To test complex relational queries and referential integrity enforcement without manual fixture construction, \texttt{SyntheticProviderRegistryGenerator} provides five canonical graph topology builders:

\begin{enumerate}
    \item \textbf{Solo Practitioner Graph (\texttt{generateSoloPractitioner(seed)})}: Constructs an isolated practitioner bound to a single healthcare organization and primary practice location via a single \texttt{PractitionerRole}.
    \item \textbf{Multi-Role Specialist Graph (\texttt{generatePractitionerWithMultipleRoles(seed)})}: Constructs a medical specialist operating across multiple distinct hospital networks and consulting suites, generating multiple \texttt{PractitionerRole} entities referencing independent organizations.
    \item \textbf{Healthcare Organization Graph (\texttt{generateOrganizationWithLocations(seed)})}: Builds a comprehensive enterprise hospital network comprising headquarters, regional campuses, specialized wings, and technical communication endpoints.
    \item \textbf{Connected Provider Network (\texttt{generateProviderNetwork(seed)})}: Constructs an exhaustive clinical ecosystem linking all seven resource types: \texttt{Organization}, \texttt{Location}, \texttt{Endpoint}, \texttt{HealthcareService}, \texttt{Practitioner}, \texttt{PractitionerRole}, and multidisciplinary clinical \texttt{Group}.
    \item \textbf{Invalid Reference Graph (\texttt{generateInvalidReferenceGraph(seed)})}: Deliberately constructs resources containing broken and dangling logical references (such as non-existent target \texttt{Organization/org-missing-999}) for automated negative testing of referential validation and \texttt{OperationOutcome} rejection pipelines.
\end{enumerate}

\begin{lstlisting}[language=Java, caption={Constructing Topological Graphs with SyntheticProviderRegistryGenerator}]
// Build deterministic complete provider network topology
long seed = 42L;
SyntheticProviderRegistryGenerator generator = new SyntheticProviderRegistryGenerator(seed);
SyntheticProviderRegistryGenerator.ProviderRegistryGraph network = generator.generateProviderNetwork(seed);

// Access strongly typed generated resources
Organization org = network.getOrganization();
Practitioner pract = network.getPractitioner();
PractitionerRole role = network.getPractitionerRole();
Location location = network.getLocation();
HealthcareService service = network.getHealthcareService();
Endpoint endpoint = network.getEndpoint();
Group group = network.getGroup();
\end{lstlisting}

% =============================================================================
% SECTION A.10: THEMIS SECURITY SIMULATION & AUTHORIZATION FIXTURES
% =============================================================================
\section{Themis Security Simulation \& Authorization Fixtures}
\label{sec:paradeigma_security}

Harmonia's \textbf{Themis} security engine governs role-based access control (RBAC), fine-grained attribute-based access control (ABAC), and dual-authority asynchronous change verification. \texttt{Paradeigma} provides standardized security actor fixtures and context factories in \texttt{net.fhirfactory.harmonia.paradeigma.common.security} to facilitate comprehensive positive and negative authorization testing.

\subsection{Standard Security Actors Catalog}

The \texttt{ParadeigmaSecurityActors} catalog defines seven standard principals, mapped roles, granted authorities, and operational boundaries, as detailed in Table~\ref{tab:paradeigma_security_actors}.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l p{2.5cm} p{3.5cm} X}
\toprule
\textbf{Actor Name} & \textbf{Harmonia Roles} & \textbf{Granted Authorities} & \textbf{Permitted Functional Boundary} \\
\midrule
\textbf{Provider Steward} & \texttt{PRV\_ADM}, \texttt{PRV\_SUB}, \texttt{PRV\_APR}, \texttt{PRV\_RDR} & \texttt{provider.change.submit}, \texttt{provider.change.approve}, \texttt{provider.read}, \texttt{provider.search}, \texttt{provider.admin} & Full directory governance: synchronous read/search, change request submission, and change approval across all directory resources. \\
\addlinespace
\textbf{Clinician} & \texttt{PRV\_RDR} & \texttt{provider.read}, \texttt{provider.search} & Clinical query operations: synchronous read and search. Write or update submissions are rejected with HTTP 403 Forbidden. \\
\addlinespace
\textbf{System Administrator} & \texttt{SYS\_ADM}, \texttt{PRV\_ADM} & \texttt{system.admin}, \texttt{provider.admin} & Infrastructure management, pipeline monitoring, security label maintenance, and administrative override. \\
\addlinespace
\textbf{Integration Service} & \texttt{SYS\_INT}, \texttt{PRV\_PROC} & \texttt{system.integration}, \texttt{provider.change.process} & Automated messaging queues, Ponos Ergon asynchronous execution, and inter-service dispatch. \\
\addlinespace
\textbf{Read-Only User} & \texttt{PRV\_RDR} & \texttt{provider.read} & Resource inspection and single-resource retrieval; search querying restricted. \\
\addlinespace
\textbf{Unauthorized User} & \textit{None} & \textit{None} & Authenticated identity without directory permissions; all protected API calls evaluate to \texttt{DENY}. \\
\addlinespace
\textbf{Unauthenticated} & \textit{None} & \textit{None} & Anonymous identity; rejected at gateway ingress with HTTP 401 Unauthorized. \\
\bottomrule
\end{tabularx}
\caption{Paradeigma Themis Security Actors Reference Catalog}
\label{tab:paradeigma_security_actors}
\end{table}

\subsection{Security Context Orchestration (\texttt{SecurityScenarioContext})}

Test scenarios utilize \texttt{SecurityScenarioContext} to inject validated security credentials, caller correlation identifiers, and client identifiers directly into HTTP requests and Camel exchange headers:

\begin{lstlisting}[language=Java, caption={Orchestrating Security Scenario Contexts and Themis Authorization}]
// Create Provider Steward context with auto-generated correlation ID
SecurityScenarioContext stewardContext = SecurityScenarioContext.providerSteward();

// Create Clinician context with explicit correlation ID
SecurityScenarioContext clinicianContext = SecurityScenarioContext.clinician("corr-audit-901");

// Evaluate authorization against Themis policy evaluator
ThemisResource resource = ThemisResource.of(
    "Practitioner",
    "pract-101",
    "PROVIDER_REGISTRY",
    Set.of(ThemisSecurityLabel.of("PROVIDER_REGISTRY"))
);

ThemisAuthorizationRequest authRequest = ThemisAuthorizationRequest.builder()
    .principal(stewardContext.principal())
    .authorities(stewardContext.grantedAuthorities())
    .action(ThemisAction.CREATE)
    .target(resource)
    .context(stewardContext.securityContext())
    .build();

ThemisAuthorizationDecision decision = evaluator.authorize(authRequest);
assertThat(decision.isAllowed()).isTrue();
\end{lstlisting}

\subsection{Negative Testing Authorization Seams}

To verify defensive platform behavior when encountering security anomalies, \texttt{ParadeigmaSecurityActors} exposes context factories that inject specific security faults:
\begin{itemize}
    \item \textbf{Expired Context (\texttt{ParadeigmaSecurityActors.expiredContext()})}: Simulates an expired bearer token or session context where \texttt{expirationTime} is in the past, validating that gateway interceptors reject the interaction before routing.
    \item \textbf{Revoked Authority (\texttt{ParadeigmaSecurityActors.revokedAuthorityContext(...)})}: Simulates a steward whose \texttt{provider.change.submit} authority has been revoked mid-session, verifying policy rejection.
    \item \textbf{Missing Role (\texttt{ParadeigmaSecurityActors.missingRoleContext(...)})}: Simulates a principal with partial roles missing mandatory execution authorities (\texttt{PRV\_SUB}), confirming granular RBAC boundary enforcement.
\end{itemize}

% =============================================================================
% SECTION A.11: PHI-AWARE LOGGING SIMULATION & CREDENTIAL PROTECTION
% =============================================================================
\section{PHI-Aware Logging Simulation \& Credential Protection}
\label{sec:paradeigma_logging}

In accordance with Harmonia's dual-gate PHI logging policy (Appendix~\ref{app:phi_logging}), operational logs (\texttt{INFO}, \texttt{WARN}, \texttt{ERROR}) must remain strictly devoid of clinical payloads, patient identifiers, and credentials under all circumstances.

\subsection{Dual-Gate Verification Probe (\texttt{PhiLogTestProbe})}

The \texttt{PhiLogTestProbe} provides an in-memory, auto-closeable Logback appender probe designed for automated assertion of logging compliance during scenario execution:

\begin{lstlisting}[language=Java, caption={Dual-Gate Assertion Flow using PhiLogTestProbe}]
try (PhiLogTestProbe probe = PhiLogTestProbe.startCapture()) {
    // 1. Execute system operation under test
    gatewayController.createResource("Practitioner", fhirJson, correlationId, "pas", "steward", null);

    // 2. Assert that no PHI attributes leaked into operational logs
    probe.assertNoPhiInOperationalLogs("Dr. Elena Vance", "8003610000000042");

    // 3. Assert that PHI is routed to diagnostic destination when dual-gate is active
    probe.assertPhiPresentInDiagnosticLogs("Dr. Elena Vance");
    probe.assertPhiMarkerAttachedToDiagnosticLogs();

    // 4. Assert zero leakage of credentials or sensitive tokens across ALL log destinations
    probe.assertNoSecretsInAnyLog(SecretLeakageAssertion.getStandardSyntheticSecrets());
}
\end{lstlisting}

\subsection{Synthetic Secret Leakage Protection (\texttt{SecretLeakageAssertion})}

Authentication credentials, connection strings, and crypto keys must never appear in any log sink, regardless of whether diagnostic logging is active. \texttt{SecretLeakageAssertion} generates comprehensive synthetic sentinel values to validate zero-leakage constraints across:
\begin{itemize}
    \item Synthetic JWT Bearer and OAuth tokens (\texttt{eyJhbGciOiJIUzI1Ni...}).
    \item Platform API secret keys (\texttt{harmonia\_sec\_live\_...}).
    \item Database credentials and PostgreSQL connection strings.
    \item ActiveMQ Artemis message broker passwords.
    \item RSA and PKCS\#8 private key blocks (\texttt{-----BEGIN PRIVATE KEY-----}).
\end{itemize}

\subsection{Integrated Multi-Capability Verification Listing}

Listing~\ref{lst:complete_paradeigma_test} provides a complete, worked exemplar demonstrating the coordinated usage of synthetic generators, security contexts, governed gateway interactions, and PHI logging probes.

\begin{lstlisting}[language=Java, caption={Multi-Capability Integration Test: Registry + Security + PHI Logging}, label={lst:complete_paradeigma_test}]
@Test
@DisplayName("Integrated Verification: Seeded Generation + Themis Ingress + Governed Workflow + PHI Isolation")
void testCompleteGovernedLifecycleWithSecurityAndLogging() throws Exception {
    long seed = 42L;
    String correlationId = "corr-exemplar-042";

    // Step 1: Initialize deterministic synthetic generator
    PractitionerGenerator generator = new PractitionerGenerator(seed);
    Practitioner practitioner = generator.generateValid("pract-dr-vance-42");
    String familyName = practitioner.getNameFirstRep().getFamily();
    String hpii = practitioner.getIdentifierFirstRep().getValue();
    String fhirJson = FhirContext.forR5().newJsonParser().encodeResourceToString(practitioner);

    // Step 2: Establish Provider Steward Security Context
    SecurityScenarioContext steward = SecurityScenarioContext.providerSteward();
    ThemisResource resource = ThemisResource.of(
        "Practitioner", "pract-dr-vance-42", "PROVIDER_REGISTRY",
        Set.of(ThemisSecurityLabel.of("PROVIDER_REGISTRY"))
    );

    // Step 3: Verify Themis Ingress Policy Authorization
    ThemisAuthorizationRequest authReq = ThemisAuthorizationRequest.builder()
        .principal(steward.principal())
        .authorities(steward.grantedAuthorities())
        .action(ThemisAction.CREATE)
        .target(resource)
        .context(steward.securityContext())
        .build();
    ThemisAuthorizationDecision decision = policyEvaluator.authorize(authReq);
    assertThat(decision.isAllowed()).isTrue();

    // Step 4: Attach PHI Test Probe & Execute Governed Ingress
    PhiLoggingConfig.setPhiEnabled(true);
    try (PhiLogTestProbe probe = PhiLogTestProbe.startCapture()) {
        ResponseEntity<String> postResp = gatewayController.createResource(
            "Practitioner", fhirJson, correlationId, "pas",
            steward.principal().principalId(), null
        );
        assertThat(postResp.getStatusCode().value()).isEqualTo(202);

        // Step 5: Process Governed Workflow via Ponos Ergon
        String location = postResp.getHeaders().getLocation().toString();
        String pragmaId = location.substring(location.lastIndexOf('/') + 1);
        Pragma pragma = pragmaCacheService.getPragma(pragmaId).orElseThrow();
        practitionerErgon.processErgon(pragma, camelContext.createProducerTemplate().getDefaultEndpoint().createExchange());
        assertThat(pragma.getStatus()).isEqualTo(PragmaStatus.COMPLETED);

        // Step 6: Validate Synchronous Read via Mnemosyne
        ResponseEntity<String> getResp = gatewayController.readResource("Practitioner", "pract-dr-vance-42", null);
        assertThat(getResp.getStatusCode().value()).isEqualTo(200);

        // Step 7: Assert Strict Dual-Gate Logging Policy & Secret Protection
        probe.assertNoPhiInOperationalLogs(familyName, hpii);
        probe.assertPhiPresentInDiagnosticLogs(familyName, hpii);
        probe.assertPhiMarkerAttachedToDiagnosticLogs();
        probe.assertNoSecretsInAnyLog(SecretLeakageAssertion.getStandardSyntheticSecrets());
    }
}
\end{lstlisting}

% =============================================================================
% SECTION A.12: COMPREHENSIVE CONFIGURATION REFERENCE MATRIX
% =============================================================================
\section{Comprehensive Configuration Reference Matrix}
\label{sec:paradeigma_config_matrix}

The Paradeigma simulation suite and its integrated security/logging harnesses are controlled through standard Spring Boot properties, JVM system properties, and container environment variables.

\subsection{Simulator Service Properties \& Port Allocations}

Table~\ref{tab:paradeigma_config_matrix} lists the core runtime parameters across the simulation microservices.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l l c X}
\toprule
\textbf{Configuration Property} & \textbf{Environment Variable} & \textbf{Default} & \textbf{Description} \\
\midrule
\texttt{server.port} (PAS) & \texttt{SERVER\_PORT} & \texttt{8091} & REST management port for \texttt{paradeigma-pas}. \\
\texttt{server.port} (EMR) & \texttt{SERVER\_PORT} & \texttt{8092} & REST management port for \texttt{paradeigma-emr}. \\
\texttt{server.port} (LMS) & \texttt{SERVER\_PORT} & \texttt{8093} & REST management port for \texttt{paradeigma-lms}. \\
\texttt{server.port} (RIS-PAC) & \texttt{SERVER\_PORT} & \texttt{8094} & REST management port for \texttt{paradeigma-rispac}. \\
\texttt{server.port} (Scenarios) & \texttt{SERVER\_PORT} & \texttt{8090} & REST orchestration port for \texttt{paradeigma-scenarios}. \\
\texttt{server.port} (FHIR Gateway) & \texttt{SERVER\_PORT} & \texttt{8089} & REST FHIR R5 Provider Registry gateway port. \\
\addlinespace
\texttt{paradeigma.pas.generation-mode} & \texttt{PAS\_GEN\_MODE} & \texttt{OFF} & Scheduling mode: \texttt{OFF}, \texttt{FIXED}, or \texttt{RANDOM\_RANGE}. \\
\texttt{paradeigma.pas.interval-ms} & \texttt{PAS\_INTERVAL\_MS} & \texttt{5000} & Pacing interval in milliseconds when operating in \texttt{FIXED} mode. \\
\texttt{paradeigma.pas.target-host} & \texttt{PAS\_TARGET\_HOST} & \texttt{localhost} & Inbound Harmonia Pylai MLLP bridge hostname. \\
\texttt{paradeigma.pas.target-port} & \texttt{PAS\_TARGET\_PORT} & \texttt{2101} & Inbound Harmonia Pylai MLLP bridge port for \texttt{PD-01}. \\
\addlinespace
\texttt{paradeigma.emr.mllp-port} & \texttt{EMR\_MLLP\_PORT} & \texttt{2201} & Inbound MLLP port for \texttt{PD-05} ADT demographics ingestion. \\
\texttt{paradeigma.lms.mllp-port-adt} & \texttt{LMS\_MLLP\_PORT\_ADT} & \texttt{2202} & Inbound MLLP port for \texttt{PD-06} ADT demographics ingestion. \\
\texttt{paradeigma.lms.mllp-port-orm} & \texttt{LMS\_MLLP\_PORT\_ORM} & \texttt{2204} & Inbound MLLP port for \texttt{PD-08} pathology order routing. \\
\texttt{paradeigma.rispac.mllp-port-adt} & \texttt{RISPAC\_MLLP\_PORT\_ADT} & \texttt{2203} & Inbound MLLP port for \texttt{PD-07} ADT demographics ingestion. \\
\texttt{paradeigma.rispac.mllp-port-orm} & \texttt{RISPAC\_MLLP\_PORT\_ORM} & \texttt{2205} & Inbound MLLP port for \texttt{PD-09} radiology order routing. \\
\bottomrule
\end{tabularx}
\caption{Paradeigma Simulator Microservice Configuration Properties}
\label{tab:paradeigma_config_matrix}
\end{table}

\subsection{Security \& PHI Logging Governance Configuration}

Table~\ref{tab:security_logging_config} details the governance switches controlling Themis policy evaluation and PHI log routing.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l l c X}
\toprule
\textbf{Configuration Property} & \textbf{Environment Variable} & \textbf{Default} & \textbf{Description} \\
\midrule
\texttt{harmonia.logging.phi-enabled} & \texttt{HARMONIA\_LOGGING\_PHI\_ENABLED} & \texttt{false} & Global security gate. When \texttt{true}, permits PHI diagnostic logs to route to \texttt{org.harmonia.phi}. \\
\addlinespace
\texttt{themis.security.enabled} & \texttt{THEMIS\_SECURITY\_ENABLED} & \texttt{true} & Enables declarative Themis RBAC/ABAC policy enforcement across gateway ingress controllers. \\
\addlinespace
\texttt{themis.audit.trail.enabled} & \texttt{THEMIS\_AUDIT\_TRAIL\_ENABLED} & \texttt{true} & Emits tamper-evident security audit records for all authorization evaluations and policy decisions. \\
\bottomrule
\end{tabularx}
\caption{Security \& PHI Logging Governance Properties}
\label{tab:security_logging_config}
\end{table}

\subsection{FailureSimulator Fault Injection Knobs}

Table~\ref{tab:fault_injection_knobs} summarizes the fault injection switches configurable dynamically via \texttt{FaultInjectionConfig}.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{l c c X}
\toprule
\textbf{Property Key} & \textbf{Type / Range} & \textbf{Default} & \textbf{Simulated Fault Behavior} \\
\midrule
\texttt{paradeigma.fault.drop-connection-prob} & $0.0 - 1.0$ & \texttt{0.0} & Probability of resetting active TCP connection before transmitting frame. \\
\texttt{paradeigma.fault.no-ack-prob} & $0.0 - 1.0$ & \texttt{0.0} & Probability of dropping response to trigger socket read timeout in caller. \\
\texttt{paradeigma.fault.ack-delay-ms} & $\ge 0$\,ms & \texttt{0} & Artificial latency injected prior to returning acknowledgment frame. \\
\texttt{paradeigma.fault.application-error-prob} & $0.0 - 1.0$ & \texttt{0.0} & Probability of returning \texttt{MSA-1=AE} (transient error). \\
\texttt{paradeigma.fault.application-reject-prob} & $0.0 - 1.0$ & \texttt{0.0} & Probability of returning \texttt{MSA-1=AR} (schema/validation rejection). \\
\texttt{paradeigma.fault.malformed-segment-prob} & $0.0 - 1.0$ & \texttt{0.0} & Probability of corrupting message framing or segment delimiters. \\
\texttt{paradeigma.fault.duplicate-msh10-prob} & $0.0 - 1.0$ & \texttt{0.0} & Probability of duplicating \texttt{MSH-10} to test idempotency filters. \\
\bottomrule
\end{tabularx}
\caption{FailureSimulator Fault Injection Configuration Knobs}
\label{tab:fault_injection_knobs}
\end{table}

% =============================================================================
% SECTION A.13: STEP-BY-STEP CLI & REST EXECUTION INSTRUCTIONS
% =============================================================================
\section{Step-by-Step CLI \& REST Execution Instructions}
\label{sec:paradeigma_execution_guide}

This section provides concrete, copy-pasteable instructions for building, running, and orchestrating the Paradeigma simulation suite across diverse operating environments.

\subsection{Building the Solution via Maven}

To compile all core platform and Paradeigma simulator modules:

\begin{lstlisting}[language=bash, caption={Building Harmonia and Paradeigma Modules}]
# Build entire repository without running tests
mvn clean install -DskipTests

# Build only the Paradeigma simulation subsystem
mvn clean install -pl paradeigma -am -DskipTests

# Run all unit and component tests across Paradeigma
mvn test -pl paradeigma/paradeigma-common,paradeigma/paradeigma-scenarios
\end{lstlisting}

\subsection{Running Simulators as Standalone Microservices}

Each simulator microservice can execute locally as an independent Spring Boot process across separate terminal windows:

\begin{lstlisting}[language=bash, caption={Launching Standalone Simulator Microservices}]
# Terminal 1: Launch Patient Administration System Simulator (Port 8091)
mvn spring-boot:run -pl paradeigma/paradeigma-pas

# Terminal 2: Launch Electronic Medical Record Simulator (Port 8092, MLLP 2201)
mvn spring-boot:run -pl paradeigma/paradeigma-emr

# Terminal 3: Launch Laboratory Information System Simulator (Port 8093, MLLP 2202/2204)
mvn spring-boot:run -pl paradeigma/paradeigma-lms

# Terminal 4: Launch Radiology Information System & PACS (Port 8094, MLLP 2203/2205)
mvn spring-boot:run -pl paradeigma/paradeigma-rispac

# Terminal 5: Launch Scenario Conductor Engine (Port 8090)
mvn spring-boot:run -pl paradeigma/paradeigma-scenarios
\end{lstlisting}

\subsection{Docker Compose Topology Orchestration}

To deploy the unified simulation and platform environment using Docker Compose:

\begin{lstlisting}[language=bash, caption={Docker Compose Lifecycle Management}]
# Navigate to the Paradeigma deployment directory
cd paradeigma/deployment

# Build images and start all containers in background
docker compose -f docker-compose-paradeigma.yml up --build -d

# Check runtime health and container port status
docker compose -f docker-compose-paradeigma.yml ps

# Tail aggregated logs across the simulation fleet
docker compose -f docker-compose-paradeigma.yml logs -f

# Gracefully stop and tear down containers
docker compose -f docker-compose-paradeigma.yml down -v
\end{lstlisting}

\subsection{REST API Scenario Orchestration (cURL Snippets)}

The \texttt{paradeigma-scenarios} engine and individual simulator management APIs can be controlled via standard HTTP cURL commands:

\begin{lstlisting}[language=bash, caption={REST Orchestration cURL Snippets}]
# 1. Trigger an immediate single 9-step Patient Journey
curl -i -X POST http://localhost:8090/api/scenarios/journey

# 2. Trigger concurrent journeys under the high-speed TEST profile
curl -i -X POST "http://localhost:8090/api/scenarios/concurrent?count=10&profile=TEST"

# 3. Trigger concurrent journeys under human-paced DEMO profile
curl -i -X POST "http://localhost:8090/api/scenarios/concurrent?count=3&profile=DEMO"

# 4. Query scenario conductor status and execution counters
curl -s http://localhost:8090/api/scenarios/status | jq .

# 5. Halt all active background scenarios
curl -i -X POST http://localhost:8090/api/scenarios/stop

# 6. Trigger discrete PAS hospital registration
curl -i -X POST http://localhost:8091/api/pas/register \
  -H "Content-Type: application/json" \
  -d '{"patientId":"MRN-90901","firstName":"Jane","lastName":"Doe","gender":"F"}'

# 7. Ingress FHIR R5 Practitioner Create via Pylai Gateway (Port 8089)
curl -i -X POST http://localhost:8089/Practitioner \
  -H "Content-Type: application/fhir+json" \
  -H "X-Correlation-ID: corr-curl-001" \
  -H "X-Source-System: pas" \
  -H "X-Security-Principal: steward" \
  -d '{
    "resourceType": "Practitioner",
    "id": "pract-curl-01",
    "identifier": [{
      "system": "http://ns.electronichealth.net.au/id/hi/hpii/1.0",
      "value": "8003610000000042"
    }],
    "name": [{"family": "Bowman", "given": ["Francis"]}]
  }'
\end{lstlisting}

% =============================================================================
% SECTION A.14: AUTOMATED SCENARIO & INTEGRATION TEST SUITES
% =============================================================================
\section{Automated Scenario \& Integration Test Suite Reference}
\label{sec:paradeigma_test_suites}

The \texttt{paradeigma-test} module houses comprehensive JUnit 5 integration and scenario test suites validating end-to-end platform capabilities:

\begin{itemize}
    \item \textbf{\texttt{ProviderRegistryLifecycleScenarioTest}}: Validates governed directory resource creation, ETag version updates, optimistic locking conflicts (HTTP 412 Precondition Failed), and referential rejection with \texttt{OperationOutcome}.
    \item \textbf{\texttt{ProviderRegistrySecurityScenarioTest}}: Exercises Themis RBAC/ABAC policy boundaries across all 7 security actors, validating privilege enforcement and negative failure seams (expired tokens, revoked authorities).
    \item \textbf{\texttt{ProviderRegistryLoggingScenarioTest}}: Validates dual-gate logging isolation, ensuring PHI diagnostic messages attach the \texttt{PHI} marker and zero secrets leak across logs.
    \item \textbf{\texttt{ProviderRegistryCrossCapabilityE2ETest}}: Executes coordinated end-to-end multi-capability journeys binding synthetic generation, Themis authorization, Ponos Ergon asynchronous execution, Mnemosyne relational storage, and PHI test probe assertions.
    \item \textbf{\texttt{RecoveryAcceptanceTest}}: Validates broker journal durability, worker crash recovery, and outbound exponential retry backoff.
    \item \textbf{\texttt{ThemisDefenceInDepthAcceptanceTest}}: Validates dual-gate security evaluation across gateway ingress, messaging dispatch, and asynchronous Ergon execution.
\end{itemize}

\begin{lstlisting}[language=bash, caption={Executing Paradeigma Automated Integration Test Suites}]
# Execute the complete Provider Registry scenario suite
mvn test -pl paradeigma/paradeigma-test \
  -Dtest=ProviderRegistryLifecycleScenarioTest,ProviderRegistrySecurityScenarioTest,ProviderRegistryLoggingScenarioTest,ProviderRegistryCrossCapabilityE2ETest

# Execute platform resilience and crash-recovery acceptance tests
mvn test -pl paradeigma/paradeigma-test -Dtest=RecoveryAcceptanceTest

# Execute Themis defense-in-depth security verification tests
mvn test -pl paradeigma/paradeigma-test -Dtest=ThemisDefenceInDepthAcceptanceTest
\end{lstlisting}
